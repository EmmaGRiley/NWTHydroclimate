% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SWE_summary.R
\name{regional_boxplot}
\alias{regional_boxplot}
\title{Regional boxplot of yearly mean SWE}
\usage{
regional_boxplot(
  surface,
  flags,
  curyear,
  act,
  save_path,
  plot_name,
  select_year_point_size = 2,
  historic_point_size = 1,
  legend_position = c(0.1, 0.95),
  save = FALSE,
  plot_width = 16,
  plot_height = 10,
  y_min = NA,
  y_max = NA,
  numbersites
)
}
\arguments{
\item{surface}{Surface type to analyze (e.g., "upland", "lake")}

\item{flags}{Data quality flags to exclude (e.g., c("Y", "HS"))}

\item{curyear}{Current year to highlight (e.g., 2024)}

\item{act}{Activity status to include (e.g., "A" for active)}

\item{save_path}{Path to save plot (if save = TRUE)}

\item{plot_name}{Name for saved plot file}

\item{select_year_point_size}{Size of point for current year (default: 2)}

\item{historic_point_size}{Size of points for historic years (default: 1)}

\item{legend_position}{Position of legend (default: c(0.1, 0.95))}

\item{save}{Logical, if TRUE save plot to file (default: FALSE)}

\item{plot_width}{Plot width in cm (default: 16)}

\item{plot_height}{Plot height in cm (default: 10)}

\item{y_min}{Minimum y-axis value (optional)}

\item{y_max}{Maximum y-axis value (optional)}

\item{numbersites}{Minimum number of sites required per region}
}
\value{
ggplot2 plot object showing boxplots of yearly mean SWE by region,
with current year highlighted in blue
}
\description{
Creates a boxplot of yearly mean snow water equivalent (SWE) separated by region,
with the current year highlighted in blue. Historic years are shown as gray points.
}
\examples{
# Create regional boxplot
plot <- regional_boxplot(
  surface = "upland",
  flags = c("Y", "HS"),
  curyear = 2024,
  act = "A",
  save_path = "C:/Figures",
  plot_name = "Regional_boxplot",
  select_year_point_size = 2,
  historic_point_size = 1.3,
  legend_position = c(0.5, 0.95),
  save = TRUE,
  plot_width = 18,
  plot_height = 11,
  y_min = NA,
  y_max = NA,
  numbersites = 3
)
}

