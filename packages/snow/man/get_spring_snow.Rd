% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ERA5_spring_snow_analysis.R
\name{get_spring_snow}
\alias{get_spring_snow}
\title{Download and process spring snow depth for a single year}
\usage{
get_spring_snow(year)
}
\arguments{
\item{year}{Year to download data for (e.g., 2024)}
}
\value{
SpatRaster object from terra package containing snow depth data for the specified year
}
\description{
Downloads ERA5-Land snow depth data for a specified year using the KrigR package.
This function is used internally by ERA5_spring_snow_analysis.R to download
hourly snow depth data for spring months (typically March-May, but dates are
configurable in the script).
}
\details{
This function requires:
\itemize{
  \item KrigR package installed
  \item ECMWF API credentials (api_user and api_key)
  \item Spatial extent defined (study_extent)
  \item File naming convention (file variable)
}

The function downloads hourly data and saves it as a NetCDF file in the current
working directory.
}
\examples{
# Note: This function requires KrigR and API credentials
# It is typically called within a loop in ERA5_spring_snow_analysis.R

# Set up required variables first:
# api_user <- "your_email@example.com"
# api_key <- "your_api_key"
# study_extent <- terra::ext(c(-140, -95, 51, 80))
# file <- "Dec31"

# Then download data for a year:
# snow_data <- get_spring_snow(2024)
}

