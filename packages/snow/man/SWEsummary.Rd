% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SWE_summary.R
\name{SWEsummary}
\alias{SWEsummary}
\title{Summary statistics for snow water equivalent data}
\usage{
SWEsummary(
  data,
  surface,
  minyear,
  flags,
  maxyear,
  curmaxyear,
  act,
  write,
  hdensity,
  ldensity
)
}
\arguments{
\item{data}{Data frame with snow survey data. Must contain columns:
region, site, year, swe_cm, snow_depth_cm, longitude, latitude, surface_type,
data_flag_1, data_flag_2, density, activity}

\item{surface}{Surface type to analyze (e.g., "upland", "lake")}

\item{minyear}{Minimum year for baseline period (e.g., 2000)}

\item{flags}{Data quality flags to exclude (e.g., c("Y", "HS", "Sk", "P", "Sk_2"))}

\item{maxyear}{Maximum year for baseline period (e.g., 2021)}

\item{curmaxyear}{Current year for comparison (e.g., 2025)}

\item{act}{Activity status to include (e.g., "A" for active)}

\item{write}{Logical, if TRUE write results to CSV file}

\item{hdensity}{High density threshold (calculated automatically if not provided)}

\item{ldensity}{Low density threshold (e.g., 0.1)}
}
\value{
Data frame with summary statistics including:
\itemize{
  \item region: Geographic region
  \item site: Site name
  \item yrs: Total years of record
  \item yrs2: Years in baseline period
  \item meanSWE01_20: Mean SWE for baseline period (mm)
  \item meandepth_cur: Mean depth for current year (cm)
  \item meanSWE_cur: Mean SWE for current year (mm)
  \item meanSWE01_cur: Mean SWE from minyear to current (mm)
  \item meanSWE_allyears: Mean SWE for all years (mm)
  \item pernorm: Percent normal relative to baseline period
  \item pernorm_allyrs: Percent normal relative to all years
  \item rank2024: Rank of current year
  \item Long: Longitude
  \item Lat: Latitude
}
}
\description{
Calculates summary statistics for snow water equivalent (SWE) data including
mean values, percent normal calculations, and rankings. Automatically calculates
density thresholds based on data distribution.
}
\examples{
# Calculate summary statistics
summary_table <- SWEsummary(
  data = snow_data,
  surface = "upland",
  minyear = 2000,
  maxyear = 2021,
  curmaxyear = 2025,
  flags = c("Y", "HS", "Sk", "P", "Sk_2"),
  ldensity = 0.1,
  act = "A",
  write = FALSE
)
}

